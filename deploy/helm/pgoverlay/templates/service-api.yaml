apiVersion: v1
kind: Service
metadata:
  name: {{ include "pgoverlay.fullname" . }}-api
  labels:
    {{- include "pgoverlay.labels" . | nindent 4 }}
spec:
  type: {{ .Values.api.service.type }}
  selector:
    {{- include "pgoverlay.selectorLabels" . | nindent 4 }}
  ports:
    - name: api
      port: {{ .Values.api.port }}
      targetPort: api
